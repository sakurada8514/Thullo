import { BaseLayout } from "components/layout/BaseLayout";
import { memo } from "react";

export const BoardDetail = memo(() => (
  <>
    <BaseLayout>
      <div className="container mx-auto px-4 py-10">
        <p>ss</p>
      </div>
    </BaseLayout>
  </>
));

BoardDetail.displayName = "BoardDetail";
