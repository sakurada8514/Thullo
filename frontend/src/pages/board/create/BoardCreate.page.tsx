import { memo } from "react";
import { BoardCreateContainer } from "./containers";

export const BoardCreatePage = memo(() => <BoardCreateContainer />);
BoardCreatePage.displayName = "BoardCreatePage";
